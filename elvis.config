[
 {
   elvis,
   [
    {config,
     [#{dirs => ["src"],
        filter => "*.erl",
        ruleset => erl_files,
        rules => [
            {elvis_style, line_length, #{limit => 100, skip_comments => whole_line}},
            {elvis_style, nesting_level, #{level => 4}}
        ]
      },
      #{dirs => ["."],
        filter => "*.rebar.config",
        ruleset => rebar_config
      },
      #{dirs => ["."],
        filter => "elvis.config",
        ruleset => elvis_config
      }
     ]
    }
   ]
 }
].
